## What

One pattern per PR. Small beats clever.

## Checklist

- [ ] Stdlib only — no new dependencies
- [ ] Read-only — no deletes, writes only via explicit `--out` paths
- [ ] Test added, `pytest` green
- [ ] Honest about coverage (docs updated if a framework gap changed)
